* if using npm *

Rename .env.local ke .env
npm install
npm run dev

How to activate front-end capability for "reset-password"

cd frontend
npm install
npm run build


* if using pnpm *

npm install -g pnpm

Rename .env.local ke .env
pnpm i
pnpm dev

How to activate front-end capability for "reset-password"

cd frontend
pnpm i
pnpm build
